# ============================================================
# TripJoy AI Service — Python Dependencies
# ============================================================
# Install: pip install -r requirements.txt
# Dev install: pip install -r requirements.txt -r requirements-dev.txt
# ============================================================

# --- Web Framework ---
fastapi==0.115.5
uvicorn[standard]==0.32.1     # includes websockets, httptools, watchfiles

# --- Data Validation ---
pydantic==2.9.2
pydantic-settings==2.7.0
python-dotenv==1.0.1

# --- Google Cloud / Vertex AI (Gemini) ---
google-cloud-aiplatform==1.70.0
google-auth==2.36.0
vertexai                       # Gemini 1.5 Flash via Vertex AI
google-genai==1.18.0


# --- LangGraph & LangChain ---
langgraph==1.0.4
langgraph-prebuilt==1.0.5

# --- HTTP Client ---
requests==2.32.3
httpx==0.28.1                  # Async HTTP client (used by FastAPI TestClient & future refactors)

# --- Google Places API ---
# Uses `requests` directly (no extra package needed)
# google-maps-services-python==4.10.0  # Uncomment if switching to official SDK

# --- Route Optimization ---
ortools==9.11.4210             # Google OR-Tools — TSP/VRP route optimization

# --- Wikipedia Data ---
wikipedia-api==0.7.1           # Fetch destination info for TravelNotebook

# --- MCP (Model Context Protocol) & Database ---
psycopg2-binary==2.9.9         # PostgreSQL adapter for Python (used in MCP tools)

mcp==1.9.4

